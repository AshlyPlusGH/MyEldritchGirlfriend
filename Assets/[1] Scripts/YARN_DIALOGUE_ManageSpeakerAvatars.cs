using UnityEngine;
using UnityEngine.AI;
using Yarn.Unity;
using Yarn.Unity.UnityLocalization;

/*
public class YARN_DIALOGUE_ManageSpeakerAvatars : MonoBehaviour
{
    [SerializeField] private bool debug;
        private void Log(string contents){ if (debug){ Debug.Log(contents + " at: " + name); }}

    [Space(10)]

    [SerializeField] private Dialou dialogueRunner;

    void Awake(){ Setup(); }
    public void Setup(){  }

    void UpdateUI()
    {
        GetLine();
    }

    private void OnEnable()
    {
        RegisterListeners();
    }
    private void RegisterListeners(){ dialogueRunner.on.AddListener(OnNodeCompleted); }
    private void OnDisable()
    {
        UnregisterListeners();
    }
    private void UnregisterListeners(){ dialogueRunner.onNodeComplete.AddListener(OnNodeCompleted); }

    public void UpdateAvatars(LocalizedLine line)
    {
        
    }

}

*/